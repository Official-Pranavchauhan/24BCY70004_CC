class Solution:
    def containsNearbyDuplicate(self, nums: List[int], k: int) -> bool:
        prev_index = {}

        for i in range(len(nums)):
            if nums[i] in prev_index and i - prev_index[nums[i]] <= k:
                return True

            prev_index[nums[i]] = i

        return False
